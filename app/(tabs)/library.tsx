import { useMemo, useState } from "react";
import { FlatList, StyleSheet, Text, View } from "react-native";
import { MaterialIcons } from "@expo/vector-icons";
import { ScreenContainer } from "@/components/screen-container";
import { Card, IconButton, palette, SearchBox, SectionTitle } from "@/components/trennix-ui";
import { exercises, MuscleGroup } from "@/lib/trennix/data";

const filters: ("Todos" | MuscleGroup)[] = ["Todos", "Peito", "Costas", "Pernas", "Ombros", "Braços"];
export default function LibraryScreen() {
  const [query, setQuery] = useState(""); const [filter, setFilter] = useState<(typeof filters)[number]>("Todos");
  const results = useMemo(() => exercises.filter((item) => (filter === "Todos" || item.muscle === filter) && item.name.toLowerCase().includes(query.toLowerCase())), [filter, query]);
  return <ScreenContainer className="px-5 pt-3"><View style={styles.header}><View><Text style={styles.kicker}>CONHEÇA SEU CORPO</Text><Text style={styles.title}>Biblioteca</Text></View><IconButton name="filter-list" /></View><SearchBox value={query} onChangeText={setQuery} placeholder="Buscar exercícios"/><FlatList data={filters} horizontal showsHorizontalScrollIndicator={false} keyExtractor={(item) => item} contentContainerStyle={styles.filters} renderItem={({ item }) => <Text onPress={() => setFilter(item)} style={[styles.filter, filter === item && styles.filterActive]}>{item}</Text>} /><SectionTitle title={`${results.length} exercícios`} action="Mais filtros" /><FlatList data={results} keyExtractor={(item) => item.id} showsVerticalScrollIndicator={false} contentContainerStyle={styles.list} renderItem={({ item }) => <Card style={styles.exercise}><View style={styles.exerciseIcon}><MaterialIcons name="fitness-center" size={21} color={palette.lime}/></View><View style={styles.copy}><Text style={styles.name}>{item.name}</Text><Text style={styles.meta}>{item.muscle} · {item.equipment}</Text><Text style={styles.detail}>{item.sets} séries · {item.reps} reps</Text></View><MaterialIcons name="chevron-right" color={palette.muted} size={21}/></Card>} /></ScreenContainer>;
}
const styles = StyleSheet.create({ header: { flexDirection: "row", justifyContent: "space-between", alignItems: "center", marginBottom: 20 }, kicker: { color: palette.lime, fontSize: 10, fontWeight: "900", letterSpacing: 1.3 }, title: { color: palette.text, fontSize: 29, fontWeight: "900", marginTop: 5 }, filters: { gap: 7, paddingVertical: 16 }, filter: { color: palette.muted, paddingHorizontal: 14, paddingVertical: 9, borderRadius: 20, backgroundColor: palette.surface, overflow: "hidden", fontSize: 12, fontWeight: "800" }, filterActive: { color: palette.bg, backgroundColor: palette.lime }, list: { paddingBottom: 30 }, exercise: { flexDirection: "row", alignItems: "center", padding: 13, marginBottom: 9 }, exerciseIcon: { width: 43, height: 43, borderRadius: 14, backgroundColor: "#243522", justifyContent: "center", alignItems: "center" }, copy: { flex: 1, marginLeft: 12 }, name: { color: palette.text, fontSize: 14, fontWeight: "800" }, meta: { color: palette.muted, marginTop: 3, fontSize: 11 }, detail: { color: palette.lime, marginTop: 7, fontSize: 11, fontWeight: "700" }, });
