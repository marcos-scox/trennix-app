import { Platform } from "react-native";
import * as SecureStore from "expo-secure-store";
const KEY="trennix.ai-key.v1";
export async function readAiKey(){try{return Platform.OS==="web"?window.sessionStorage.getItem(KEY):await SecureStore.getItemAsync(KEY)}catch{return null}}
export async function saveAiKey(value:string){const key=value.trim();if(Platform.OS==="web"){if(key)window.sessionStorage.setItem(KEY,key);else window.sessionStorage.removeItem(KEY);return}if(key)await SecureStore.setItemAsync(KEY,key);else await SecureStore.deleteItemAsync(KEY)}
export async function askTrennixAI(question:string,context:string){const key=await readAiKey();if(!key)throw new Error("Cole sua chave da IA em Configurações para conversar com o treinador.");const response=await fetch("https://api.groq.com/openai/v1/chat/completions",{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Bearer ${key}`},body:JSON.stringify({model:"llama-3.3-70b-versatile",temperature:.35,messages:[{role:"system",content:`Você é o treinador inteligente do TRENNIX. Responda em português do Brasil, com objetividade e segurança. Não prescreva diagnóstico médico. Contexto do atleta: ${context}`},{role:"user",content:question}]})});if(!response.ok)throw new Error("A IA não respondeu. Confira sua chave e tente novamente.");const data=await response.json();return data?.choices?.[0]?.message?.content??"Não consegui gerar uma resposta agora."}
